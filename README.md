- To build the project, just issue make.
- make rectype to build the recursive "part".
- execute either native (or byte) executable by issuing ./typage.native
(or ./typage.byte)
- make test followed by ./runtests to run the test suites.
